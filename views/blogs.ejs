<% include partials/top %>

<div id="homeContainer" class="container">

  <h1 class="text-center">Blogs List</h1>

  <a class="btn btn-lg btn-outline-primary btn-block mb-3" href="/blogs/new">New Blog</a>

  <% blogs.forEach((blog) => { %>
  <div class="row mb-3">
    
    <a href="/blogs/<%= blog._id %>/view">
      <div class="card">

        <img src="<%= blog.image %>" class="card-img-top">
        
        <div class="card-body">
          
          <h4 class="card-title"><%= blog.title %></h4>

          <p class="card-text text-dark"><%= blog.body %></p>

        </div>

      </div>

    </a>

  </div>
  <% }) %>

</div>

<% include partials/bottom %>
